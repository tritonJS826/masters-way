{
  "name": "mw-storage",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "make build",
    "start": "nodemon --watch . --exec 'make start' --signal SIGTERM --ext go,json,yaml,sql,env,sh",
    "serve": "make start",
    "test:integration": "make test",
    "installAll": "make install",
    "swag": "make swag",
    "db:run": "make db-run",
    "db:generate": "make db-schema",
    "help": "make help",
    "api:generate": "pnpm concurrently \"pnpm run api:generate:storage\"",
    "api:generate:storage": "openapi-generator-cli generate -g go -i http://localhost:8003/docs/doc.json -o ./apiAutogenerated/storage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "3.1.4"
  }
}
