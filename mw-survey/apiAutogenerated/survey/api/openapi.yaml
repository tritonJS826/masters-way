openapi: 3.0.1
info:
  contact: {}
  title: Masters way survey API
  version: "1.0"
servers:
- url: /survey
paths:
  /dev/reset-db:
    get:
      description: resets db
      responses:
        "204":
          content: {}
          description: No Content
      summary: resets db
      tags:
      - dev
  /looking-for-mentor:
    post:
      description: Post survey looking for mentor
      operationId: survey-looking-for-mentor
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/mwsurvey_internal_schemas.PostSurveyLookingForMentorPayload'
        description: query params
        required: true
      responses:
        "204":
          content: {}
          description: No Content
      summary: Post survey looking for mentor
      tags:
      - survey
      x-codegen-request-body-name: request
  /user-intro:
    post:
      description: Post survey user intro
      operationId: survey-user-intro
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/mwsurvey_internal_schemas.PostSurveyUserIntroPayload'
        description: query params
        required: true
      responses:
        "204":
          content: {}
          description: No Content
      summary: Post survey user intro
      tags:
      - survey
      x-codegen-request-body-name: request
components:
  schemas:
    mwsurvey_internal_schemas.PostSurveyLookingForMentorPayload:
      properties:
        currentExperience:
          type: string
        mentorDescription:
          type: string
        skillsToLearn:
          type: string
        userEmail:
          type: string
      required:
      - currentExperience
      - mentorDescription
      - skillsToLearn
      - userEmail
      type: object
    mwsurvey_internal_schemas.PostSurveyUserIntroPayload:
      properties:
        deviceId:
          type: string
        preferredInterfaceLanguage:
          type: string
        promoCode:
          type: string
        role:
          type: string
        source:
          type: string
        studentExperience:
          type: string
        studentGoals:
          type: string
        whyRegistered:
          type: string
      required:
      - deviceId
      - preferredInterfaceLanguage
      - promoCode
      - role
      - source
      - studentExperience
      - studentGoals
      - whyRegistered
      type: object
x-original-swagger-version: "2.0"
