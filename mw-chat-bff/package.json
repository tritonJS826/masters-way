{
  "name": "mw-chat-bff",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "make build",
    "start": "make start",
    "test": "make test",
    "installDependencies": "make install",
    "db-run": "make db-run",
    "help": "make help",
    "api:generate": "pnpm concurrently \"pnpm run api:generate:general\" \"pnpm run api:generate:chat\"",
    "api:generate:general": "openapi-generator-cli generate -g go -i http://localhost:8000/docs/doc.json -o ./apiAutogenerated/general",
    "api:generate:chat": "openapi-generator-cli generate -g go -i http://localhost:8001/docs/doc.json -o ./apiAutogenerated/chat"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
