import 'package:flutter/material.dart';

class PersonalAreaScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Personal Area'),
      ),
      body: Center(
        child: Text('This is the Personal Area screen'),
      ),
    );
  }
}
