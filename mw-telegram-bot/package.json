{
  "name": "mw-telegram-bot",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "make build",
    "start": "nodemon --watch . --exec 'make start' --signal SIGTERM --ext go,json,yaml,env,sh",
    "serve": "make start",
    "test": "make test",
    "install": "make install",
    "api:generate": "openapi-generator-cli generate -g go -t ../go-common/openapi-templates/ -i ../mw-general-bff/docs/swagger.json -o ./apiAutogenerated/general",
    "help": "make help"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "3.1.4"
  }
}
