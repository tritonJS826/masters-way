import {SchemasUploadFileResponse} from "src/apiAutogenerated/chat";
import {FileModel} from "src/model/businessModel/File";

/**
 * Convert {@link chatDTO} to {@link Room}
 */
export const fileDTOToFile = (fileDTO: SchemasUploadFileResponse): FileModel => {
  return new FileModel({
    ...fileDTO,
    uuid: fileDTO.id,
    url: fileDTO.srcUrl,
  });
};
