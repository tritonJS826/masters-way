import {MwTrainingBffInternalSchemasTopicPreview} from "src/apiAutogenerated/training";
import {TopicPreview} from "src/model/businessModelPreview/TopicPreview";

// /**
//  * Map tree topics
//  */
// export const mapTreeTopicsPreview = (topicA: MwServerInternalSchemasMetricTreeNode): TopicPreview => {

//   const mappedTopic = new TopicPreview({
//     children: topicA.children.map(mapTreeTopics),
//     name: topicA.metric.description,
//     parentUuid: topicA.metric.parentUuid,
//     uuid: topicA.metric.uuid,
//     order,
//     createdAt,
//     owner,
//     practiceMaterialAmount,
//     theoryMaterialAmount,
//     trainingUuid,
//   });

//   if (topicA.children && topicA.children.length > 0) {
//     mappedTopic.children = topicA.children.map(mapTreeTopics); // Recursively map children
//   }

//   return mappedTopic;
// };

/**
 * Convert {@link topicDTO} to {@link TopicPreview}
 */
export const topicDTOToTopic = (
  topicDTO: MwTrainingBffInternalSchemasTopicPreview,
): TopicPreview => {
  return new TopicPreview({
    ...topicDTO,
    createdAt: new Date(topicDTO.createdAt),
    // Children: topicDTO.children.map(mapTreeTopicsPreview),
    children: [],
  });
};
