import {SchemasUserPlainResponse} from "src/apiAutogenerated/general";
import {UserPlain} from "src/model/businessModel/User";

/**
 * Convert {@link UserPlainDTO} to {@link UserPlain}
 */
export const userPlainDTOToUserPlainConverter = (userPlainDTO: SchemasUserPlainResponse): UserPlain => {
  return new UserPlain({
    ...userPlainDTO,
    createdAt: new Date(userPlainDTO.createdAt),
  });
};
