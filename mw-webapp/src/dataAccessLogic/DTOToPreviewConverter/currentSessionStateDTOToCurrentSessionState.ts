import {MwTestWebsocketInternalSchemasUserJoinedSessionEventResponse} from "src/apiAutogenerated/testWebsocket";
import {CurrentSessionState} from "src/model/businessModel/CurrentSessionState";

/**
 * Convert {@link currentSessionStateDTO} to {@link CurrentSessionState}
 */
export const currentSessionStateDTOToCurrentSessionState = (
  currentSessionStateDTO: MwTestWebsocketInternalSchemasUserJoinedSessionEventResponse,
): CurrentSessionState => {
  return new CurrentSessionState({currentUsers: currentSessionStateDTO.currentUsers});
};
