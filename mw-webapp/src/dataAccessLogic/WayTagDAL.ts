import {DeleteWayTagRequest} from "src/apiAutogenerated";
import {WayTag} from "src/model/businessModelPreview/WayTag";
import {WayTagService} from "src/service/WayTagService";

/**
 * Params for{@link WayTagDAL.addWayTagToWay}
 */
interface addWTagToWayParams {

  /**
   * Tag name
   */
  name: string;

  /**
   * Way uuid
   */
  wayUuid: string;
}

/**
 * Provides methods to interact with the way tags
 */
export class WayTagDAL {

  /**
   * Create way tag
   */
  public static async addWayTagToWay(params: addWTagToWayParams): Promise<WayTag> {
    const wayTagDTO = await WayTagService.addWayTagToWay({request: params});

    const wayTag = new WayTag({...wayTagDTO});

    return wayTag;
  }

  /**
   * Delete way tag by UUID
   */
  public static async deleteWayTag(requestParameters: DeleteWayTagRequest): Promise<void> {
    await WayTagService.deleteWayTag(requestParameters);
  }

}

