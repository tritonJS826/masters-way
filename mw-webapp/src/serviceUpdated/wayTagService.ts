import {
  CreateWayTagRequest,
  DeleteWayTagRequest,
  GetWayTagsByWayUuidRequest,
  SchemasWayTagResponse,
  UpdateWayTagRequest,
} from "src/apiAutogenerated";
import {wayTagService} from "src/serviceUpdated/services";

/**
 * Provides methods to interact with the way tags
 */
export class WayTagService {

  /**
   * Create way tag
   */
  public static async createWayTag(requestParameters: CreateWayTagRequest): Promise<SchemasWayTagResponse> {
    const wayTag = await wayTagService.createWayTag(requestParameters);

    return wayTag;
  }

  /**
   * Get way tags by way UUID
   */
  public static async GetWayTags(requestParameters: GetWayTagsByWayUuidRequest): Promise<SchemasWayTagResponse[]> {
    const wayTags = await wayTagService.getWayTagsByWayUuid(requestParameters);

    return wayTags;
  }

  /**
   * Update way tag
   */
  public static async updateWayTag(requestParameters: UpdateWayTagRequest): Promise<SchemasWayTagResponse> {
    const updatedWayTag = await wayTagService.updateWayTag(requestParameters);

    return updatedWayTag;
  }

  /**
   * Delete way tag by UUID
   */
  public static async deleteWayTag(requestParameters: DeleteWayTagRequest): Promise<void> {
    await wayTagService.deleteWayTag(requestParameters);
  }

}

