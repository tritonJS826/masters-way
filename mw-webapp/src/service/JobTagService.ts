import {
  CreateJobTagRequest,
  DeleteJobTagRequest,
  SchemasJobTagResponse,
  UpdateJobTagRequest,
} from "src/apiAutogenerated";
import {jobTagService} from "src/service/services";

/**
 * Provides methods to interact with the job tags
 */
export class JobTagService {

  /**
   * Create job tag
   */
  public static async createJobTag(requestParameters: CreateJobTagRequest): Promise<SchemasJobTagResponse> {
    const jobTag = await jobTagService.createJobTag(requestParameters);

    return jobTag;
  }

  /**
   * Update job tag
   */
  public static async updateJobTag(requestParameters: UpdateJobTagRequest): Promise<SchemasJobTagResponse> {
    const updatedJobTag = await jobTagService.updateJobTag(requestParameters);

    return updatedJobTag;
  }

  /**
   * Delete way tag by UUID
   */
  public static async deleteJobTag(requestParameters: DeleteJobTagRequest): Promise<void> {
    await jobTagService.deleteJobTag(requestParameters);
  }

}
