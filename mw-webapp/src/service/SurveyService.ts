import {SurveyUserIntroRequest} from "src/apiAutogenerated/survey";
import {surveyService} from "src/service/services";

/**
 * Provides methods to interact with the surveys
 */
export class SurveyService {

  /**
   * Survey user intro
   */
  public static async surveyUserIntro(requestParameters: SurveyUserIntroRequest): Promise<void> {
    await surveyService.surveyUserIntro(requestParameters);
  }

  /**
   * Survey find a mentor request
   */
  public static async findMentor(): Promise<void> {
    // Await surveyService.surveyUserIntro(requestParameters);
  }

}
