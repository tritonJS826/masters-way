import {
  CreateJobTagRequest,
  DeleteJobTagRequest,
  MwServerInternalSchemasJobTagResponse,
  UpdateJobTagRequest,
} from "src/apiAutogenerated/general";
import {labelService} from "src/service/services";

/**
 * Provides methods to interact with the labels
 */
export class LabelService {

  /**
   * Create label
   */
  public static async createLabel(requestParameters: CreateJobTagRequest): Promise<MwServerInternalSchemasJobTagResponse> {
    const label = await labelService.createJobTag(requestParameters);

    return label;
  }

  /**
   * Update label
   */
  public static async updateLabel(requestParameters: UpdateJobTagRequest): Promise<MwServerInternalSchemasJobTagResponse> {
    const updatedLabel = await labelService.updateJobTag(requestParameters);

    return updatedLabel;
  }

  /**
   * Delete label by UUID
   */
  public static async deleteLabel(requestParameters: DeleteJobTagRequest): Promise<void> {
    await labelService.deleteJobTag(requestParameters);
  }

}
