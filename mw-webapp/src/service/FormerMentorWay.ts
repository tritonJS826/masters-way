import {CreateFormerMentorWayRequest} from "src/apiAutogenerated";
import {formerMentorWayService} from "src/service/services";

/**
 * Provides methods to interact with the former mentors
 */
export class FormerMentorWayService {

  /**
   * Create former mentor way
   */
  public static async createFormerMentor(requestParameters: CreateFormerMentorWayRequest): Promise<void> {
    await formerMentorWayService.createFormerMentorWay(requestParameters);
  }

}

