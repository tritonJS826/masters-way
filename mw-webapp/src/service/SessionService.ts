import {
  MwTrainingBffInternalSchemasCreateSessionResult,
  SessionPostRequest,
} from "src/apiAutogenerated/training";
import {sessionService} from "src/service/services";

/**
 * Provides methods to interact with the Sessions
 */
export class SessionService {

  /**
   * Create session
   */
  public static async createSession(
    requestParameters: SessionPostRequest,
  ): Promise<MwTrainingBffInternalSchemasCreateSessionResult> {
    const session = await sessionService.sessionPost(requestParameters);

    return session;
  }

}

