import {CreateSessionRequest, MwTrainingBffInternalSchemasTestSession} from "src/apiAutogenerated/training";
import {sessionService} from "src/service/services";

/**
 * Provides methods to interact with the Sessions
 */
export class SessionService {

  /**
   * Create session
   */
  public static async createSession(
    requestParameters: CreateSessionRequest,
  ): Promise<MwTrainingBffInternalSchemasTestSession> {
    const session = await sessionService.createSession(requestParameters);

    return session;
  }

}

