import {CreatePlanLabelRequest, DeletePlanLabelRequest} from "src/apiAutogenerated/general";
import {planLabelService} from "src/service/services";

/**
 * Provides methods to interact with the PlanLabels
 */
export class PlanLabelService {

  /**
   * Create plan label
   */
  public static async createPlanLabel(requestParameters: CreatePlanLabelRequest): Promise<void> {
    await planLabelService.createPlanLabel(requestParameters);
  }

  /**
   * Delete plan label by UUID
   */
  public static async deletePlanLabel(requestParameters: DeletePlanLabelRequest): Promise<void> {
    await planLabelService.deletePlanLabel(requestParameters);
  }

}
