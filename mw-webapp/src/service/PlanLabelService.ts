import {CreatePlanJobTagRequest, DeletePlanJobTagRequest} from "src/apiAutogenerated/general";
import {planLabelService} from "src/service/services";

/**
 * Provides methods to interact with the PanLabels
 */
export class PlanLabelService {

  /**
   * Create plan label
   */
  public static async createPlanLabel(requestParameters: CreatePlanJobTagRequest): Promise<void> {
    await planLabelService.createPlanJobTag(requestParameters);
  }

  /**
   * Delete plan label by UUID
   */
  public static async deletePlanLabel(requestParameters: DeletePlanJobTagRequest): Promise<void> {
    await planLabelService.deletePlanJobTag(requestParameters);
  }

}
