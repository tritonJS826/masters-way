import {TrainingTestPostRequest} from "src/apiAutogenerated/training";
import {trainingTestService} from "src/service/services";

/**
 * Provides methods to interact with the TrainingTests
 */
export class TrainingTestService {

  /**
   * Create training test
   */
  public static async createTrainingTest(requestParameters: TrainingTestPostRequest): Promise<void> {
    await trainingTestService.trainingTestPost(requestParameters);
  }

}

