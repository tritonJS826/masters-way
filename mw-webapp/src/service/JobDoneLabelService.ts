import {CreateJobDoneJobTagRequest, DeleteJobDoneJobTagRequest} from "src/apiAutogenerated/general";
import {jobDoneLabelService} from "src/service/services";

/**
 * Provides methods to interact with the JobDoneLabels
 */
export class JobDoneLabelService {

  /**
   * Create jobDone label
   */
  public static async createJobDoneLabel(requestParameters: CreateJobDoneJobTagRequest) {
    return await jobDoneLabelService.createJobDoneJobTag(requestParameters);
  }

  /**
   * Delete jobDone label by UUID
   */
  public static async deleteJobDoneLabel(requestParameters: DeleteJobDoneJobTagRequest): Promise<void> {
    await jobDoneLabelService.deleteJobDoneJobTag(requestParameters);
  }

}
