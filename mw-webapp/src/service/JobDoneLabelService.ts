import {CreateJobDoneLabelRequest, DeleteJobDoneLabelRequest} from "src/apiAutogenerated/general";
import {jobDoneLabelService} from "src/service/services";

/**
 * Provides methods to interact with the JobDone labels
 */
export class JobDoneLabelService {

  /**
   * Create jobDone label
   */
  public static async createJobDoneLabel(requestParameters: CreateJobDoneLabelRequest): Promise<void> {
    return await jobDoneLabelService.createJobDoneLabel(requestParameters);
  }

  /**
   * Delete jobDone label by UUID
   */
  public static async deleteJobDoneLabel(requestParameters: DeleteJobDoneLabelRequest): Promise<void> {
    await jobDoneLabelService.deleteJobDoneLabel(requestParameters);
  }

}
